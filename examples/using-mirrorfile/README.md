# Mirrorfile Example

An example using the [mirrorfile](https://github.com/n-at-han-k/mirrorfile) ruby gem.

## Why?

The reason for doing this would be to use the latest git version. This is helpful for development as you can modify the openkrill code and rebuild your server to run the updates.
